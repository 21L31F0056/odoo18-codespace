<?xml version="1.0" encoding="utf-8"?>
<sdwot>

    <record model="ir.ui.view" id="view_move_form_inherit_other_charges">
        <field name="name">account.move.form</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <field name="payment_reference" position="after">
                <field name="exchange_rate"
                       invisible="currency_id == company_currency_id" readonly="state != 'draft'"
                       force_save="1" groups="base.group_multi_currency"
                       store="True"/>
                <field name="inverse_exchange_rate" invisible="currency_id == company_currency_id" readonly="1"
                       force_save="1" store="True" groups="base.group_multi_currency"/>
            </field>
            <xpath expr="//page[@name='invoice_tab']/field[@name='invoice_line_ids']/tree" position="inside">
                <field name="currency_rate" invisible="1"/>
            </xpath>
            <page name="invoice_tab" position="after">
                <page id="invoice_tab_in_cc" invisible="currency_id == company_currency_id or move_type =='entry'">
                    <field name="invoice_line_ids_in_cc" widget="section_and_note_one2many" mode="tree">
                        <tree editable="bottom" string="Journal Items" create="0" delete="0">
                            <field name="currency_id" column_invisible="True"/>
                            <field name="company_currency_id" column_invisible="True"/>
                            <field name="company_id" column_invisible="True"/>
                            <field name="product_id" widget="many2one_barcode" options="{'no_create': True}"/>
                            <field name="name" widget="section_and_note_text" optional="show"/>
                            <field name="account_id" groups="account.group_account_readonly"
                                   options="{'no_create': True}"/>
                            <field name="quantity"/>
                            <field name="price_unit_in_cc" string="Price"/>
                            <field name="price_subtotal_in_cc" string="Subtotal"/>
                            <field name="price_total_in_cc" string="Total"/>
                        </tree>
                    </field>
                    <group col="12" class="oe_invoice_lines_tab">
                        <group colspan="8">
                            <field name="narration" placeholder="Terms and Conditions" colspan="2" nolabel="1"/>
                        </group>
                        <!-- Totals (only invoices / receipts) -->
                        <group colspan="4">
                            <group class="oe_subtotal_footer"
                                   invisible="move_type not in ('out_invoice', 'out_refund', 'in_invoice', 'in_refund', 'out_receipt', 'in_receipt') or payment_state == 'invoicing_legacy'">
                                <field name="amount_untaxed_unsigned" colspan="2" string="Untaxed Amount"
                                       readonly="state != 'draft' or (move_type not in ('in_invoice', 'in_refund', 'in_receipt') and not quick_edit_mode)"/>
                                <field name="amount_total_unsigned" string="Total Amount"
                                       readonly="state != 'draft' or (move_type not in ('in_invoice', 'in_refund', 'in_receipt') and not quick_edit_mode)"/>
                                <field name="amount_residual_unsigned" class="oe_subtotal_footer_separator"
                                       string="Amount Due"
                                       invisible="state == 'draft'"/>
                            </group>
                        </group>
                    </group>
                </page>
            </page>

        </field>
    </record>

</sdwot>
